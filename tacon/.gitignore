__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
.venv/
venv/

# Default output dir for `tacon dashboard` (when --out is omitted).
# Real publishes pass --out explicitly; this catches the convenience-default path.
tacon-dashboard/

# Local user state
~/.tacon/
*.db
*.db-journal

# Local secrets — never commit
.env
.env.local
